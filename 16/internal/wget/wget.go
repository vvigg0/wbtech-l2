package wget

// Run запускает процесс зеркалирования сайта согласно переданной конфигурации.
// Функция выполняет обход ссылок, скачивает страницы и ресурсы,
// сохраняет их в локальный каталог и переписывает ссылки внутри HTML.
// Возвращает ошибку, если не удалось инициализировать или выполнить обход.
func Run(cfg *Config) error {
	return crawl(cfg)
}
